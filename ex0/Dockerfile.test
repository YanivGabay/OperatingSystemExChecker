# Dockerfile.test
FROM rockylinux/rockylinux:8

# Install necessary development tools and dependencies
RUN dnf install -y gcc gcc-c++ valgrind tar python3 && \
    dnf clean all

# Verify GCC version
RUN gcc --version
